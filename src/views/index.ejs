<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/mainDash.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>

<body>
    <%- include('navbar', LoginData) %>
        <h1>INFO AUTOR</h1>
        <%= LoginData %>

            <h1>ILUSTRACIONES SIGUIENDO</h1>

            <div class="container">
                <% for( let index=0; index < array_illust.length; index++ ) { %>
                    <% if (array_illust[index].length> 4) { %>
                        <div class="row">
                            <% for( let indexa=0; indexa < array_illust[index].length; indexa++ ) { %>
                                <div class="col-sm">
                                    <h4 class="">
                                        <%= array_illust[index][indexa].id %>
                                    </h4>
                                    <h4 class="">
                                        <%= array_illust[index][indexa].title %>
                                    </h4>
                                    <img src="/images<%= array_illust[index][indexa].thumb_dir.split(".")[1] %>.jpg"
                                    style="width:30%">
                                </div>
                                <% } %>
                        </div>
                        <% } %>
                            <% } %>
            </div>
</body>

</html>